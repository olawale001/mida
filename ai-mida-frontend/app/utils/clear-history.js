import { useState } from 'react';
import api from '../lib/api';

const handleClearHistory= async () => {
    const []
    const token = localStorage.getItem('/clear-history/', {
        headers: {
            Authorization: `Bearer ${token}`
        },
    });

}